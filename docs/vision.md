# Vision

`chi-tea-aye` is a small C++20 Sokol game prototype built around a vertical slice first.

The near-term goal is a working loop: open a window, render simple geometry, read input, expose state through a debug panel, and keep each module small enough to replace or extend later.

The project codename is technical and does not decide the final game title.
